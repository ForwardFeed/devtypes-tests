
# Setup

## When Installing the project
```sh
git submodule init
git submodule update
```

## To keep up todate with devtypes repository

```sh
git submodule update
```


## To run the project
I use bunJS and Linux

This is to autogen the tests, so no type is forgotten (unless you forget the module)
```sh
bun autogen
```